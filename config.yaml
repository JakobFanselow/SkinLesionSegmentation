training:
  seed: 42
  batch_size: 16
  train_percentage: 0.7
  validation_percentage: 0.15
  test_percentage: 0.15
  learning_rate: 3e-4
  epochs: 20

paths:
  image_dir: ""
  mask_dir: ""

images:
  resolution: 512

loader:
  workers: 8